% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deque.R, R/zzz.R
\name{deque}
\alias{deque}
\alias{Deque}
\title{Double Ended Queue}
\usage{
deque(items = NULL)

Deque(items = NULL)
}
\arguments{
\item{items}{a list of items}
}
\description{
\code{deque} creates a double ended queue.
\code{Deque} is deprecated and will be removed from future releases.
}
\details{
Following methods are exposed:
\preformatted{
.$push(item)
.$pushleft(item)
.$pop()
.$popleft()
.$peek()
.$peekleft()
.$extend(q)
.$extendleft(q)
.$remove(item)
.$clear()
.$size()
.$as_list()
.$print()
}
\itemize{
\item \code{item}: any R object
\item \code{q}: a deque
}
}
\examples{
q <- deque()
q$push("foo")
q$push("bar")
q$pushleft("baz")
q$pop()  # bar
q$popleft()  # baz

q <- deque(list("foo", "bar"))
q$push("baz")$pushleft("bla")
}
\seealso{
\link{queue} and \link{stack}
}
